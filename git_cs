//push all new files to the git repository
git add .
git commit -m "Add all new files"
git push origin main

//up to date with the main branch before working on a new feature
git checkout main
git pull origin main
